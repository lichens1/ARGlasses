<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@color/black"
    android:layout_height="match_parent"
    tools:context=".LightActivity">

    <ImageView
        android:id="@+id/iv_back"
        android:layout_width="40dp"
        app:layout_constraintTop_toTopOf="parent"
        android:src="@drawable/ic_launcher_background"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_margin="20dp"
        android:layout_height="40dp"/>

    <ImageView
        android:id="@+id/iv_low_light"
        android:layout_width="40dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:src="@drawable/ic_launcher_background"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_margin="20dp"
        android:layout_height="40dp"/>

    <lichens.licht.arglasses.wiget.LineCentreProView
        android:id="@+id/progress"
        android:layout_width="180dp"
        android:layout_height="20dp"
        app:layout_constraintLeft_toLeftOf="@+id/iv_low_light"
        app:layout_constraintRight_toRightOf="@+id/iv_height_light"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:progress_value="50"
        app:text_show="false" />

    <ImageView
        android:id="@+id/iv_height_light"
        android:layout_width="40dp"
        android:src="@drawable/ic_launcher_background"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_margin="20dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_height="40dp"/>


</androidx.constraintlayout.widget.ConstraintLayout>